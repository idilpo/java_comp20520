/*
import java.util.Scanner;

public class CashRegister1 {
    public double money;
    public double d;

    public static void main(String[] args) {
        CashRegister CR = new CashRegister();

        Scanner in = new Scanner(System.in);
        System.out.print("Enter the amount: ");
        String money = in.nextLine();
        CashRegister(money);
        
        add(d);

    }

    public CashRegister () {
        if (money <= 0) {
            throw new IllegalArguementException("Please enter a valid input.");
        }
    }

    public add(double d) {
        if (d>0) {
            return money + d;
        } else {
            throw new IllegalArguementException("Please enter a valid input.");
        }
    }

    public remove(double d) {
        if (d>0 && money>d) {
            total = money - d;
        } else {
            throw new IllegalArguementException("Please enter a valid input.");
        }
    }
}
*/
